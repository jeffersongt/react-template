pull_request_rules:
  - name: Automated dependabot merge
    conditions:
      - author~=^dependabot(|-preview)\[bot\]$
      - status-success=lint
      - status-success=build-docker
    actions:
      merge:
        method: merge